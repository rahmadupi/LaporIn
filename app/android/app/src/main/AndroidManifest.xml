<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Firebase Auth/Firestore butuh akses internet pada build release.
         Manifest debug/profile sudah punya ini; main manifest belum. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Lokasi: dipakai geolocator di Step 3 (Pin Lokasi). FINE = GPS presisi,
         COARSE = fallback berbasis jaringan. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Kamera: dipakai image_picker di Step 2 (Ambil Foto). -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Notifikasi: wajib di Android 13+ (API 33) agar push FCM bisa tampil. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="main"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- Google Maps API key TIDAK ditulis di sini (tidak boleh masuk ke
             source control). Nilainya diinjeksi saat build dari
             manifestPlaceholders["MAPS_API_KEY"] yang dibaca Gradle dari
             local.properties (lihat android/app/build.gradle.kts &
             docs/GOOGLE_MAPS_SETUP.md). Tanpa key valid, peta tampil kosong
             tetapi aplikasi TIDAK crash — koordinat & alamat dari GPS/geocoding
             tetap berfungsi sehingga laporan tetap bisa dikirim. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}"/>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
